package middleware

const (

	// UserType is used to represent user's type. User's type can be `merchant` or `classic`
	UserType = "User Type"

	// UserLocation is used to represent merchant's location
	UserLocation = "User Location"
)
